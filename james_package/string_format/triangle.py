def triangles():
    bases = float(input("enter the base of a circle: "))
    heights = float(input("enter the height of a circle: "))
    area_of_circle = 0.5 * (bases * heights)
    print(area_of_circle)


triangles()
